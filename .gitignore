# Dependencies
/node_modules
/node_modules

# Log files
*.log

# Data files
/html_files
/errors

/dist

/Scraper/errors
/Scraper/html_files

# Local Algolia credential override (public search key, but machine-specific)
/Scraper/algolia-credentials.json

# Generated artifacts that exceed GitHub's 100 MB file limit. Both are rebuilt
# locally (index.html embeds its own compressed copy of the car data, and dev.db
# comes from `prisma migrate`), and the GitHub Pages deploy reads neither.
/all-cars-from-db.json
/prisma/prisma/*.db

# index.html IS tracked - it is the published page.

# Diagnostics scratch space (archived page snapshots, probes)
/Scraper/diag


*.png
# prisma/prisma/*.db
.DS_Store
