gemini -m gemini-2.5-flash
cd backend    
    npm run scrape
    npm run test:scrape (wont call dubizzle servers)
    npx prisma studio

    npm start

TODO:   
    spec are not updated for most cars, thats because when this project started, spec was not stored in scraper.js (and also old files in processed/unprocessed folder). but now after running scraper.js we have spec for most cars. so we need to update the db with new spec data. i suggest adding logic in save-to
